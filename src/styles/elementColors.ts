import colors from "./colors";

const elementColors = {
    activeTextColor: colors.gray100,
    disabledTextColor: colors.gray300,
    backgroundColor: colors.gray600,
    backgroundHeaderColor: colors.gray700,
    itemBackground: colors.gray200,
    itemBorderColor: colors.gray400,
}

export default elementColors;